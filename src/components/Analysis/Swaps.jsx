import React from 'react';

const Swaps = ({swaps}) => {
    return (
        <div style={{color:'whitesmoke',marginLeft:'5px',marginTop:'2px'}}>
            Swaps: {swaps}
        </div>
    );
}

export default Swaps;
