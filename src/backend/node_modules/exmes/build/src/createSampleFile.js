"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const outputStream_1 = require("./outputStream");
const NUMBERS_COUNT = 400;
const MIN_NUMBER = 0;
const MAX_NUMBER = 100;
function randomFromRange(min, max) {
    return Math.floor(Math.random() * (max - min)) + min;
}
function createSampleFile(path, separator) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        const outputStream = new outputStream_1.FileOutputStream(path, separator);
        for (let i = 0; i < NUMBERS_COUNT; i += 1) {
            const randomNumber = randomFromRange(MIN_NUMBER, MAX_NUMBER);
            yield outputStream.write(randomNumber.toString());
        }
        outputStream.end();
    });
}
exports.createSampleFile = createSampleFile;
//# sourceMappingURL=createSampleFile.js.map